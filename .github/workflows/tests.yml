name: "nix flake check --impure"
on:
  pull_request:
    branches: [master]
  push:
    branches: 'master'
jobs:
  tests:
    runs-on: ARM64
    steps:
    - uses: actions/checkout@v5
    - run: nix flake check --impure